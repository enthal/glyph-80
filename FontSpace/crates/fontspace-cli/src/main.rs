#![forbid(unsafe_code)]

//! Command-line adapter over FontSpace's typed operations.
//!
//! Empty at Milestone 0 (workspace bootstrap). The `new`, `info`, `set-pixels`,
//! `shift`, `render-text`, and `extract` subcommands land in Milestone 1.
//! See `FontSpace/spec/13-cli-and-mcp.md`.

fn main() {
    println!("fontspace: not yet implemented (Milestone 0 bootstrap)");
}
