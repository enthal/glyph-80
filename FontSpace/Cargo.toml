[workspace]
resolver = "2"
members = ["crates/*"]

# Shared package metadata. Individual crates inherit these with `field.workspace = true`.
[workspace.package]
edition = "2024"
version = "0.0.0"
license = "MIT OR Apache-2.0"
repository = "https://github.com/enthal/glyph-80"
rust-version = "1.95"
